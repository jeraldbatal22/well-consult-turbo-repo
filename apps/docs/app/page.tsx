import { Button } from "@repo/ui/components/ui/button";

export default function Home() {
  return (
    <div className=" h-screen flex justify-center items-center">
      <Button className="bg-blue-500">HELLO FROM ADMIN SIDE UPDATE</Button>
    </div>
  );
}
